---
---
<script>
  const languages = ['pt', 'en'];
  const browserLang = (navigator.language || 'pt').split('-')[0];
  const targetLang = languages.includes(browserLang) ? browserLang : 'pt';
  window.location.href = `/${targetLang}/`;
</script>
